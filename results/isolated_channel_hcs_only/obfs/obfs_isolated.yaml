env: validation_only_obfs
conversation_duration: 6030
max_start_delay: 30
one_way_availability: 30
analysis_window_size: 30
network:
  minio_net:
    network: minio
    params:
      upstream:
        latency: 0
        loss_profile: "none"
      downstream:
        latency: 0
        loss_profile: "none"
  mastodon_net:
    network: mastodon
    params:
      upstream:
        latency: 0
        loss_profile: "none"
      downstream:
        latency: 0
        loss_profile: "none"
  client_net_obfs:
    nat: false
    network: client
    params:
      upstream:
        latency: 15
        loss_profile: "none"
      downstream:
        latency: 15
        loss_profile: "none"
  server_net:
    network: server
    params:
      upstream:
        latency: 5
        loss_profile: "none"
      downstream:
        latency: 5
        loss_profile: "none"
  dns_net:
    network: dns
    params:
      upstream:
        latency: 10
        loss_profile: "none"
      downstream:
        latency: 10
        loss_profile: "none"
nodes:
  node_type_obfs:
    channel_config:
      - direction: bidirectional
        channel: obfs
        vars:
          client:
            iat: "0"
          server:
            iat: "0"
    client_per_network:
      client_net_obfs:
        quantity: 2
        profiles:
          irc_casualchat: 1
tgen:
  tgen_type_ftp:
    tgen_per_network:
      client_net_obfs:
        quantity: 6
        profiles:
          fast: .5
          slow: .5
